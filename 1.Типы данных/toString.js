// Для каждого приведенного ниже выражения, дописать, чему будет равен результат приведения к строке
// Описать в комментарии к выражению, почему именно так.

(1).toString() === ...;

({a:1}).toString() === ...;

[1].toString() === ...;
[].toString() === ...;

"".toString.toString() === ...;

(() => 1).toString() === ...;

Object.keys({a:1,b:2}).toString() === ...;
Object.entries({a:1,b:2}).toString() === ...;
Object.toString() === ...;

(1 / 0).toString() === ...;
(1 / "1").toString() === ...;

Number(1).toString() === ...;
(1 / "zero").toString() === ...;

new String(({a:1}).b) === ...;
new String(null) === ...;

({} === {}).toString() === ...;
(true).toString() === ...;